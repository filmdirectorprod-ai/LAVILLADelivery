// Safe-area insets for the mobile frame. The prototype used per-device values;
// on web we use modest fixed insets (status-bar / home-indicator breathing room).
export const SAFE_TOP = 24;
export const SAFE_BOTTOM = 16;
