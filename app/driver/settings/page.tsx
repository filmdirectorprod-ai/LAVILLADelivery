// /driver/settings — Server Component shell for the client settings screen.
// Reached from the profile screen; renders outside the bottom tab bar.
import { DriverSettingsScreen } from '@/components/driver/DriverSettingsScreen';

export default function DriverSettingsPage() {
  return <DriverSettingsScreen />;
}
